= form_for [@activation,@section] do |f|
  .name
    = f.label :name
    = f.text_field :name
  .description
    = f.label :description
    = f.text_field :description
  .submit
    = f.submit 'Create Section'

