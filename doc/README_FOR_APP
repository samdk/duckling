## Redis

Start up redis with 

  sudo redis-server /usr/local/etc/redis.conf
  
## Background Workers

Start up a worker with:

  QUEUE=* rake environment resque:work

Check progress at http://0.0.0.0:5678 